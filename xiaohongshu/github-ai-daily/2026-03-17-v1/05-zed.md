# 4. Zed 的 agent 能力还在继续往前推

今天 Zed 这边最值得注意的是：
它新增了 `spawn_agent`，也就是并行调度子代理。

这不是一个小功能。
因为它说明几件事：
- 编辑器侧也在吸收多 agent 这套思路
- agent 不再只是聊天窗口里的单线程工具
- 工作流正在更靠近“并行处理多个子任务”

再加上 GPT-5.3-Codex provider、Vercel AI Gateway、对话状态持久化这些改进，
能看出来 Zed 也在认真把 agent 能力往“编辑器内主流程”推。
