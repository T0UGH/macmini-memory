## Claude 生态工作流细节继续成熟：`context: fork` 可把 skill 放进隔离 subagent

Lydia Hallie 提醒，Claude Code 中可以通过 `context: fork` 在隔离 subagent 里运行 skill；主上下文只看到最终结果，不看到中间工具调用，同时还能通过 `agent` 字段指定子 agent 类型。

### 为什么值得看
这不是大新闻，但是真使用层面的关键技巧。它直接解决主上下文被长链路任务污染的问题，也说明 Claude 生态正在把“隔离执行 + 最终汇总”做成一等工作流。

**必看指数：★★★★☆**

### 一句话结论
**它直接解决主上下文被长链路任务污染的问题，也说明 Claude 生态正在把“隔离执行 + 最终汇总”做成一等工作流。**
